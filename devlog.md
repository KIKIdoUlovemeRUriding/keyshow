# 我用 4 块钱 + 8600 万 Token，让 AI 写了一个 Windows 按键可视化工具

> 全程零手写代码，Claude Code + DeepSeek V4 Pro 联合作战。

## 缘起

直播的时候，观众总问我按了什么键。

我说"Ctrl+C"，弹幕飘过"啥？没看清"。我说"Alt+Tab"，又有人问"主播用的什么快捷键"。

于是我决定写个工具：**按什么键就在屏幕上飘出来**。就这么简单。

但我决定做个实验——**一行代码都不自己写，全部交给 AI**。

## 选型

AI 搭档选了 **Claude Code**（Anthropic 的 CLI 编程助手），底层模型是 **DeepSeek V4 Pro**。

为什么是 DeepSeek？便宜。便宜到我愿意在它身上做实验。事实证明这个选择对得起钱包——**整个项目搞完，花了 8600 万 token，合计人民币 4 块钱**。

4 块钱什么概念？一杯蜜雪冰城柠檬水都买不到。

## 开发过程

整个过程就像跟一个永远不会累的实习生结对编程。我说想法，它写代码。出了 bug，我描述现象，它自己排查自己改。

### 第一回合：从零到一

"帮我用 .NET 实现一个小应用：在屏幕浮层显示我键盘的按键。"

Claude Code 一口气生成了项目骨架——WPF 窗口 + Win32 全局键盘钩子 + 透明置顶层。编译，运行，一片空白。

"键入字符，没有看到任何显示内容。"

它沉默三秒，回了一句：`List<T>` 换成 `ObservableCollection<T>`。

我服了。**WPF 绑定 101**，它比我还快反应过来。

### 第二回合：越改越像样

接下来就是无限的迭代循环——我说需求，它写代码，我测试，我说不对，它再改：

- "放在右下角" → 改定位
- "最多显示 5 个" → 改上限  
- "鼠标移上去不要消失" → 冻结渐隐
- "出现时加个放大特效" → BackEase 弹性动画
- "渐隐不够丝滑" → 从 Timer 跳帧改成 WPF `BeginAnimation`
- "退场时也缩小一下" → 同步 Scale + Opacity

到这一步，一个小而美的按键浮层已经像样了。但真正的考验才刚刚开始。

### 第三回合：设置面板

"创建托盘右键菜单，弹出设置面板，内容你自己发挥。"

好家伙，给 AI 自由发挥的空间，它直接给我整了一套配置系统：

- 卡片颜色（带 HSV 色盘，能拖拽取色）
- 字体切换（支持导入 .ttf/.otf）
- 尺寸、位置、语言、开机自启
- JSON 持久化到 `%LocalAppData%`

色盘第一次版本是个 hex 输入框 + 几个预设色块，我说"太低端了"，它反手就掏出 HSV 渐变幻彩画布 + 色相条 + 透明度条 + 常用色板，交互甚至比我见过的一些商业软件还好。

### 第四回合：炫技时刻

"根据按键频次，高频按键显示流光特效。"

它自己设计了方案：统计近 2 小时按键次数，高频（≥10000 次）金色、中频（≥2000 次）蓝色。特效是 `StrokeDashOffset` 动画——一段光斑沿卡片边框一笔画过，2 秒一圈，无休无止。

第一次做出来是左右平移，我说"我要绕一周"——它换了 `RotateTransform`。还是不对，"我要用笔画一周的效果"——最终换成了 Rectangle 叠加 + `StrokeDashArray`，光段沿上右下左依次跑完，完美。

### 第五回合：细节打磨

- 长按连击合并为 `A × 12` 显示
- 中英文双语支持，语言选项显示为 `中文` / `English`
- 字号、加粗、卡片最小宽度全部可调
- 打包成单文件 exe（71MB 自包含 / 290KB 依赖框架）

中间最骚的操作：自包含版 exe 双击没反应，Claude Code 从 Windows 事件查看器日志一路追到 `XamlParseException`，最终定位到 `Icon="../icon.ico"` 在单文件发布后路径解析失败。改成了嵌入资源 + `pack://application` URI，完美解决。

## 技术栈

| 层面 | 技术 |
|------|------|
| 框架 | .NET 9 / WPF + WinForms |
| 键盘钩子 | Win32 `SetWindowsHookEx(WH_KEYBOARD_LL)` |
| 动画 | `Storyboard` / `BeginAnimation` / `StrokeDashOffset` |
| 色盘 | HSV → RGB 转换 + 渐变幻彩画布 Canvas 交互 |
| 持久化 | `System.Text.Json` → `%LocalAppData%/KeyShow/` |
| 多语言 | 自建 `L` 类，运行时切换 |
| 发布 | `dotnet publish` 单文件，自包含 + 框架依赖双版本 |

## 数据总结

| 指标 | 数值 |
|------|------|
| 总 Token 消耗 | ~8600 万 |
| API 花费 | ¥4.00 |
| 源文件数 | 22 |
| 代码行数 | ~2100 |
| 人类手写代码 | **0 行** |
| 从零到 GitHub 发布 | 约 5 小时（含反复调试） |

## 反思

**AI 编程到底是什么体验？**

像有一个技术很强的同事，随叫随到，不抱怨，不摸鱼，凌晨三点也能干活。但它没有"品味"——你必须明确告诉它要什么、哪里不对、为什么不对。

你不说"透明度变化要用 WPF 动画而不是 Timer"它会一直用步进；你不说"色盘我要拖拽取色"它会给你一个 hex 输入框说做好了。

**人机协作的边界是：人负责判断，AI 负责执行。**

人对"好"的直觉——丝滑、美观、顺手——是当前 AI 最缺乏的。它能把任何技术方案实现出来，但方案的审美和用户体验，必须靠人把关。

**4 块钱值不值？**

假设一个中级 .NET 开发者日薪 800 元，5 小时开发这个项目需要 500 元。而我花了 4 块钱 + 5 小时陪着聊天。

结论：**太值了**。但前提是你能准确表达需求、快速验证结果、果断指出问题。

## 代码

GitHub: [github.com/KIKIdoUlovemeRUriding/keyshow](https://github.com/KIKIdoUlovemeRUriding/keyshow)

欢迎 Star，欢迎 PR，欢迎拿去做直播 overlay。

---

*本文由 Claude Code (Anthropic) + DeepSeek V4 Pro 协助开发，人类负责吐槽和按回车。*
